//Group of model instances

//Manually add to a collection:
add();

var dogsToWalk = new app.DogsCollection([
  spanials, pugs    
]);
dogsToWalk.add(Airedales);


//remove();
dogsToWalk.remove(pugs);
