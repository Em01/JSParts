initialize();
/* Watches for changes. Runs as soon as a model instance is created. Once an initialize function is
   attatched to a base model it will run whatever code is inside of it. Monitoring and looking out 
   for changes.
*/

get();
// Find a specific model property to use how we see fit.

set();
//Performs two functions. Change or add new propertied to models-even after we create the instance.

on();
//Trigger the changes.
