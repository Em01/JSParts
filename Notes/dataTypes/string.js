/*
A string type is used to represent textual data.
Each element in a string occupies a position in the string. The first element
is at the index of 0 with the next being one.
The length is the total number of the elements in it.

Not immutable-Once created a string cannot be modified.
It is possible to create another string based on an operation on the original string.
(subs or concat)
*/
