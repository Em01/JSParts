/*
Everything in JavaScript is an object.
It is a loosely typed and a dynamic language-You do not have to declare the
type of the variable ahead of type. This will get determined automatically when
the program is being processed. It also means the same variable can change types
*/
