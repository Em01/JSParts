const mapStateToProps (state, ownProps) {
  //maps the state to the props!
  //ownProps is the props which are passed to the component that we are wrapping-
  //exactly equal to this.props inside of the component
}
