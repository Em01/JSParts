/*
Node uses the global object as opposed to the window.

* setTimeout()/clearTimeout()/setInterval()/clearInterval()
* console.log could be accessed as global.console.log

* Variables defined in the global scope are not set on the global
object due to the modular system that node uses.


*/
